# Implementation Plan: Project Scaffolding & Skia Spike

**Branch**: `001-project-scaffold` | **Date**: 2026-07-28 | **Spec**: [spec.md](spec.md)

**Input**: Feature specification from `specs/001-project-scaffold/spec.md`

## Summary

Establish Bazel workspace, platform definitions, dependency management, and validate Skia integration via a spike binary. This phase carries the highest technical risk — Skia's build system is complex and its Bazel integration often requires significant work. The spike ensures Skia compiles and links before any downstream code depends on it.

## Technical Context

**Language/Version**: C++17

**Build System**: Bazel 6.5.0

**Primary Dependencies**: Skia (rendering), caflex (flexbox layout), googletest (testing), bazel_skylib (build helpers)

**Storage**: N/A

**Testing**: googletest

**Target Platform**: macOS ARM64 (development), Linux x86_64 (CI)

**Project Type**: C++ shared library

**Performance Goals**: N/A (scaffolding phase — no runtime performance targets)

**Constraints**: Skia must be isolated behind the render/ module; no other module may depend on Skia directly. Build must work on both macOS ARM64 and Linux x86_64.

**Scale/Scope**: Single library project with 7 internal modules (core, layout, render, surface, widgets, event, public) plus spike binary and test targets.

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

Constitution file (`.specify/memory/constitution.md`) contains placeholder template content only — no project-specific principles, constraints, or gates have been defined. Therefore:

- **Gate 1 — Project principles**: No binding principles to violate. PASS (no constitution defined).
- **Gate 2 — Constraints**: No binding constraints to violate. PASS (no constitution defined).
- **Gate 3 — Governance**: No governance rules to violate. PASS (no constitution defined).

**Verdict**: All gates pass. Constitution is a template awaiting project-specific content; no violations possible.

**Post-Design Re-check (Phase 1)**: Re-evaluated after design artifacts. No new constitution content has been added; all gates remain PASS. No violations to justify.

## Project Structure

### Documentation (this feature)

```text
specs/001-project-scaffold/
├── spec.md              # Feature specification
├── plan.md              # This file
├── research.md          # Phase 0 research output
├── data-model.md        # Phase 1 data/entity model
├── quickstart.md        # Phase 1 developer quickstart
├── contracts/           # Phase 1 interface contracts
└── tasks.md             # Phase 2 task breakdown (generated by /speckit.tasks)
```

### Source Code (repository root)

```text
native_ui/                          # Bazel workspace root
├── BUILD.bazel                     # Root alias: //:native_ui → //src/framework/public:native_ui
├── WORKSPACE                       # workspace(name = "native_ui")
├── native_ui_deps.bzl              # External dependency bootstrap
├── .bazelversion                   # 6.5.0
├── .bazelrc                        # C++17, platform aliases
├── .bazelignore                    # Ignore example workspaces
├── platforms/
│   ├── BUILD                       # config_setting + platform for macos_arm64, linux_x86_64
│   └── platforms.bzl               # Helper macros
├── third_party/
│   ├── skia/
│   │   └── BUILD.bazel             # Skia cc_library wrapper
│   └── caflex/
│       └── BUILD.bazel             # caflex cc_library wrapper
├── src/
│   └── framework/
│       ├── core/
│       │   └── BUILD.bazel         # Empty cc_library stub
│       ├── layout/
│       │   └── BUILD.bazel         # Empty cc_library stub
│       ├── render/
│       │   └── BUILD.bazel         # Empty cc_library stub
│       ├── surface/
│       │   └── BUILD.bazel         # Empty cc_library stub
│       ├── widgets/
│       │   └── BUILD.bazel         # Empty cc_library stub
│       ├── event/
│       │   └── BUILD.bazel         # Empty cc_library stub
│       └── public/
│           ├── BUILD.bazel         # Umbrella target aggregating all modules
│           └── include/
│               └── native_ui/
│                   └── native_ui_export.h  # NATIVE_UI_API macro
├── src/spike/
│   ├── BUILD.bazel                 # cc_binary depending on @skia//:skia
│   └── skia_spike.cc               # Minimal Skia draw + encode binary
├── tests/
│   ├── BUILD.bazel                 # Empty test target (test infra present)
├── spec/
│   └── native_ui/
│       └── build.yaml              # Build convention spec for agents
```

**Structure Decision**: Flat single-workspace layout mirroring graph_runtime conventions. Each framework module is a separate Bazel package. Third-party deps use `third_party/` wrappers. The spike lives in its own `src/spike/` package to isolate risk.

## Complexity Tracking

N/A — No constitution violations to justify.
